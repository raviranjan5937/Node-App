name: Release

on:
  push:
    branches: [new-github-actions]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: docker build . -t github-actions-demo-nest-app
      - run: echo "dckr_pat_zMD1wQwTx4E27pdYKBre0aiVUkU" | docker login -u raviranjan5937 --password-stdin
      - run: docker push github-actions-demo-nest-app